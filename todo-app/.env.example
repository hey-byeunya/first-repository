PORT=3000

# Supabase 프로젝트 설정 → Project Settings → API 에서 확인
SUPABASE_URL=https://your-project-ref.supabase.co
# service_role 키 (secret). 절대 프론트엔드/커밋에 노출하지 말 것 — RLS를 우회하는 서버 전용 키.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
